# tidal-ripple.nvim — Post-Audit TODO

## Completed

- [x] Save findings as todo.md
- [x] Fix config/doc drift (min_taps aligned, full config example, iterations.md updated)
- [x] Document undocumented features (sample browser, autocomplete)
- [x] **Playhead OSC integration** — flashes markers from `/dirt/play` timing
- [x] **Silent-send warning** — warns once when REPL not running
- [x] **sclang boot handshake** — sends queued until the interpreter prints output
- [x] **Dead state removed** — `state.looper_loaded`
- [x] **Sample bank data** — all 218 banks, 2,038 files (was 18/203); script now uses git trees API
- [x] README fully expanded (keymaps, prereqs, visualizer, statusline, tap tempo, JACK)
- [x] `:h tidal-ripple` help file (`doc/tidal-ripple.txt`, tags generated)
- [x] Blockwise (Ctrl-V) visual sends implemented
- [x] OSC debug logging gated behind `osc.debug` (default off)
- [x] `send_visual` aligned to function callback; redundant filetype set removed

## Remaining (nice-to-have)

- [ ] **Repo-name consistency** — plugin is `tidal-ripple.nvim`, GitHub remote is `ripple-tidal.nvim`; consider renaming repo or README.
- [ ] **Tests / CI** — no test infra exists yet.
- [ ] **Looper end-to-end test** — `iterations.md` claims implemented but unverified.

## Notes

- `BootTidal.hs` top-level `let` triggers a false-positive LSP parse error; valid GHCi script syntax.
- `doc/tags` is generated by `:helptags` — regenerate if you edit `doc/tidal-ripple.txt`.